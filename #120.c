/*
Write a program that converts all lowercase characters in a given string to its equivalent uppercase character.Write a program that converts all lowercase characters in a given string to its equivalent uppercase character.
*/

#include<stdio.h>
#include<string.h>
void main() {
char str[100];
gets(str);
printf("\n\n");
strupr(str);
puts(str);
getch();
}